#ifndef HORARIO_H
#define HORARIO_H
#include <iostream>
#include <fstream>

using namespace std;

void acomodador();
int potencia(int a, int b);
bool solonumeros(char a[]);
int conversorcharint(char num[]);
int longitud(char a[]);
bool semana(char a);

#endif // HORARIO_H
